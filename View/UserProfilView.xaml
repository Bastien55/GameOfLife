<UserControl x:Class="GameOfLife.View.UserProfilView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameOfLife.View"
             xmlns:VM="clr-namespace:GameOfLife.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800" DataContext="{Binding Source={x:Static VM:ViewModelLocator.Instance}, Path=UserVM}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Name :"/>
        <TextBox Grid.Row="1" Text="{Binding Name, UpdateSourceTrigger=LostFocus}"/>

        <Label Content="{Binding Model.Id}" HorizontalAlignment="Right"/>

        <Label Grid.Column="2" Content="Règle"/>
        <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding Rule, UpdateSourceTrigger=LostFocus}"/>
    </Grid>
</UserControl>
